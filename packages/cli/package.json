{
  "name": "@prompt-memory/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "pcheck": "dist/index.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@prompt-memory/core": "workspace:^",
    "@prompt-memory/sqlite": "workspace:^",
    "commander": "^15.0.0"
  }
}
